/*  usamos un enum porque asi si cambia el nombre de la variable, solo lo cambiamos aqui*/
export enum Configurations {
   PORT = 'PORT',
   HOST = 'HOST',
   USERNAME = 'USERNAME',
   PASSWORD = 'PASSWORD',
   DATABASE = 'DATABASE'
}
